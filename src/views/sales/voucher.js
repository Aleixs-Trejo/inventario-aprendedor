/* Función para calcular el igv en base a la siguiente fórmula:
    - igv = 18%
    - subTotal * 1.18 = totalVenta
    - totalVenta * 0.082 = subTotal
*/

const subTotal = document.querySelector(".sub__total");
const igv = document.querySelector(".igv");
const totalVenta = document.querySelector(".total__venta");

function calcularIGV() {
  // Calculamos el subTotal del precio en base al totalVenta * 0.082
  
};

calcularIGV();